DATABASE_URL="file:./dev.db"

# Local MVP safety switch.
# Leave false unless you intentionally need to test private/internal URLs.
ALLOW_PRIVATE_NETWORK_TARGETS="false"

# Optional, server-side only.
# Used by sites configured with connectionType=companion_plugin and no per-site env reference.
# WP_AGENCY_OPS_COMPANION_API_KEY=""
